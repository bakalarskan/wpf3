<Window x:Class="libraria.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:libraria"
        mc:Ignorable="d"
        Title="Libraria" Height="650" Width="1000"
        MinHeight="500" MinWidth="920"
        Icon="ikonki/book.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Background="White" Padding="2">
            <MenuItem Header="Repository"/>
            <MenuItem Header="Book">
                <MenuItem Header="Add..." Click="MenuItem_Click"/>
                <MenuItem Header="Edit..."/>
                <MenuItem Header="Delete..."/>
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1" Background="{StaticResource PrimaryColor}">
            <StackPanel Orientation="Horizontal">
                <Image Source="ikonki/book.png" Width="48" Height="48"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Libraria"
                               Foreground="{StaticResource AccentColor}"
                               FontSize="25"
                               FontFamily="Segoe Script"/>
                    <TextBlock Text="Personal Book Manager"
                               Foreground="Gray"
                               FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <ListBox Grid.Row="2">
            ItemsSource="{Binding Books}"
            BorderThickness="0"
            Background="F5F7FA"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Margin="10"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="150" Margin="10" HorizontalAlignment="Center">
                        <Border Height="180" Width="120" CornerRadius="4" Background="#E0E6ED" ClipToBounds="True">
                            <Image Source="{Binding CoverImagePath}" Stretch="UniformToFill"/>
                        </Border>
                        <TextBlock Text="{Binding Title}"
                                   FontWeight="Bold"
                                   FontSize="14"
                                   Foreground="#2B3E50"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,10,0,2"/>
                        <TextBlock Text="{Binding Author}"
                                   Foreground="Gray"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemBorder" BorderThickness="2" BorderBrush="Transparent" Background="Transparent" CornerRadius="6" Padding="2">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                        <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource PrimaryColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <Grid Grid.Row="3" Background="{StaticResource PrimaryColor}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Total: 0" Foreground="{StaticResource AccentColor}"
                       VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
